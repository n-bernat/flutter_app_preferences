name: example
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  # Depend on version from pub.dev in a real project
  flutter_app_preferences:
    path: ../

dev_dependencies:
  leancode_lint: ^14.1.0

flutter:
  uses-material-design: true
